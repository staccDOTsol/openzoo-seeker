<?xml version='1.0' encoding='utf-8'?>
<widget id="fun.openzoo.seeker" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0">
    <name>OpenZoo</name>
    <description>OpenZoo for Solana Seeker — pay-per-call chat via x402 and Mobile Wallet Adapter</description>
    <author href="https://github.com/staccDOTsol/openzoo-seeker">
        staccDOTsol
    </author>

    <!-- Local shell owns MWA; bundled chat UI lives in the iframe -->
    <content src="index.html" />

    <!-- Gateway + public Solana RPCs used by the iframe for chat/bind/stats and balance reads -->
    <allow-navigation href="https://x402-tokens.fly.dev/*" />
    <access origin="https://x402-tokens.fly.dev" />
    <allow-navigation href="https://x402.accrue.fund/*" />
    <access origin="https://x402.accrue.fund" />
    <!-- MWA identity origin. Agent IDE + billing live on zoo.openzoo.fun (/ide/session, not an open URL). -->
    <allow-navigation href="https://openzoo.fun/*" />
    <access origin="https://openzoo.fun" />
    <allow-navigation href="https://zoo.openzoo.fun/*" />
    <access origin="https://zoo.openzoo.fun" />
    <!-- Minted Agent IDE URL from POST/GET /ide/session — never a hardcoded open URL. -->
    <allow-navigation href="https://*/*" />
    <allow-navigation href="https://api.mainnet-beta.solana.com/*" />
    <access origin="https://api.mainnet-beta.solana.com" />
    <allow-navigation href="https://solana-rpc.publicnode.com/*" />
    <access origin="https://solana-rpc.publicnode.com" />

    <!-- Open wallet / external links in system browser -->
    <allow-intent href="https://*/*" />
    <!-- MWA uses solana-wallet:// scheme to discover & connect native wallet apps -->
    <allow-intent href="solana-wallet://*" />

    <!-- Fullscreen immersive mode -->
    <preference name="Fullscreen" value="true" />
    <preference name="StatusBarOverlaysWebView" value="false" />
    <preference name="StatusBarBackgroundColor" value="#000000" />
    <preference name="StatusBarStyle" value="lightcontent" />
    <preference name="KeepRunning" value="true" />
    <preference name="Orientation" value="default" />
    <preference name="BackgroundColor" value="0xff0a0a18" />
    <preference name="SplashScreenDelay" value="0" />
    <preference name="FadeSplashScreen" value="false" />
    <preference name="AutoHideSplashScreen" value="true" />
    <preference name="DisallowOverscroll" value="true" />
    <!-- Never show the raw WebView "Load failed" page. -->
    <preference name="ErrorUrl" value="error.html" />

    <!-- Android specific -->
    <platform name="android">
        <preference name="AndroidWindowSplashScreenBackground" value="#0a0a18" />
        <preference name="AndroidPersistentFileLocation" value="Compatibility" />
        <!-- Allow network from file:// origin (bundled iframe → gateway / RPC) -->
        <preference name="AndroidInsecureFileModeEnabled" value="true" />
        <!-- Immersive sticky hides nav + status bars -->
        <preference name="immersiveMode" value="true" />
        <!-- Allow video autoplay without user gesture -->
        <preference name="MediaPlaybackRequiresUserAction" value="false" />
        <!-- Use hardware back button to quit (no history to navigate) -->
        <preference name="OverrideBackButton" value="true" />
        <!-- singleTask: relaunches (e.g. returning from the wallet app) reuse the existing activity instead of recreating it -->
        <preference name="AndroidLaunchMode" value="singleTask" />

        <!-- Icons (generated from res/icon/src/token.jpg — keep) -->
        <icon density="ldpi" src="res/icon/android/ldpi.png" />
        <icon density="mdpi" src="res/icon/android/mdpi.png" />
        <icon density="hdpi" src="res/icon/android/hdpi.png" />
        <icon density="xhdpi" src="res/icon/android/xhdpi.png" />
        <icon density="xxhdpi" src="res/icon/android/xxhdpi.png" />
        <icon density="xxxhdpi" src="res/icon/android/xxxhdpi.png" />
    </platform>
</widget>
